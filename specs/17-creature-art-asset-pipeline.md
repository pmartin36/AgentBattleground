# Creature Art & Asset Pipeline

> **Superseded: folded into `66-asset-generation-api`.** Still-image generation (Z-Image via native `sd-cli`), drag-and-drop import, background removal, and the visual-style and chroma-key-color conventions now live in `66` as its image backend and pipeline. See there.
