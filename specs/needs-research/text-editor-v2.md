# TextEditor v2 (needs research)

> **Status: parked pending research.** The remaining deferred item for the engine `TextEditor` from `50-engine-text-editing-primitives`.

## Purpose

One editor capability is still unscoped.

## Remaining

- **Horizontal scroll.** The v1 editor soft-wraps long lines and scrolls vertically only. An opt-in no-wrap mode where long lines scroll sideways (with a horizontal scrollbar) is unscoped — decide whether this game's editors ever want it before speccing.

## Dependencies

- Extends `50-engine-text-editing-primitives`.
