# Creature Animation Pipeline

> **Superseded: folded into `66-asset-generation-api`.** Animation generation (MiniMax H3 via native `sd-cli`), the reference config, and the load-bearing prompt convention now live in `66` as its animation backend, with the exact commands in `experiments/creature_lab/findings/12-attack-animation-pipeline.md`. See there.
